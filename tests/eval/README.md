# Eval-Specific Tests

Distinct from tests/unit and tests/integration: these test the eval harness
itself (ml/eval/) once it exists, not the application code. See
docs/TestingStrategy.md §2 for what the eval harness needs to measure.
